Installation
============

HECATE is written in Python.
To install run the following commands:


``git clone https://github.com/telmonteiro/HECATE-DS``
``cd HECATE-DS``
``pip install -e .``

Then you can use SOAP from Python with:

``import HECATE-DS``